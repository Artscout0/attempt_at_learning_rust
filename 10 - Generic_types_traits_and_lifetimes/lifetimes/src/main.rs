/**
 * Lfetimes... Well, references are variables too, so to avoid any memory based errors, when you reach the 
 * end of a lifetime, most of the time a lifetime can be found between {}, it will remove all the referenced in that scope from memory.
 */

fn main() {
}
